# 2026-07-07 工作日志

## 功能一：工厂 3 秒实时结算
- `factoryProducts.js`: 新增 `produceBatch(ind, empMult, levelMult)` 方法，单批产量 = lineCount × empMult × levelMult × satisfaction × 0.05
- `time.js`: 新增 `factoryIntervalId`、`FACTORY_TICK_MS=3000`、`startFactoryTimer()`、`stopFactoryTimer()`、`factoryTick()` — 每 3 秒调用 `Engine.factoryTick()`
- `engine.js`: 新增 `factoryTick()` 遍历工厂产业执行实时生产；每日结算阶段3已移除工厂生产（由实时结算替代）
- `industryDetail.js`: 工厂卡片新增"生产频率：每 3 秒一批"和"预计每批产出 xxx / 3秒"

## 功能二：采矿许可证制度
- `data.js`: 删除 `mine_land` 地产类别；所有矿种新增 `licenseCost`（cost × 3）；新增 `licenseMaxLevel: 5`；移除 `landPrereqs.mining`；移除 `sectorMap.mine_land`
- `state.js`: migrate() 新增 mine_land → licenseLevel 迁移逻辑；startGame() 新增 creditRating 字段
- `engine.js`: 采矿结算检查 licenseLevel（无许可证不产出）；许可证加成 `dailyIncome × (1 + (lv-1)×0.2)`；维护成本折扣 `/ (1 + (lv-1)×0.1)`
- `industryDetail.js`: 采矿 ownedItem 显示"许可证 LvX" + 加成信息；新增"升级许可证"按钮；purchaseItem 显示"含采矿许可证"费用；Industry.buy 自动赠送 licenseLevel:1；新增 `Industry.upgradeLicense()` 方法
- `ui.js`: 移除 `mine_land` 检查

## 功能三：银行贷款信用评级 + 滑块操作
- `data.js`: bank 新增 `creditRatings`（AAA/AA/A/B）、`defaultCredit='B'`、`creditUpgradeDays=90`、`creditDowngradeDays=30`
- `state.js`: startGame()/migrate() 新增 `creditRating`、`creditDaysWithoutLoan`、`_creditLoanDays`
- `engine.js`: 新增 `updateCreditRating()` 每日评级更新；贷款利率改为使用信用评级倍率
- `deposit.js`: 完全重构 — 信用评级显示（AAA/AA/A/B）、滑块操作（存入/取出/贷款/还款）、每个滑块带实时标签更新
- `ui.js`: `_csUpdate`/`_csSet` 支持 `_labelUpdater` 自定义标签；新增 `_customLabelUpdater()` 方法；`capacitySlider` 初始化重置标签更新器
- `state.js`: totalAssets() 加入采矿许可证估值

## 功能四：员工交互优化
- `industry.js`: 精确招聘按钮改为弹出三种模式选择（免费/付费/猎头）；待分配区域每个等级直接显示【派1名】【派5名】【全派】【解雇】操作按钮，去掉"前往分配"跳转；新增 `_recruitPicker()`、`_assignUnassigned()`、`_doAssign()`、`_fireUnassignedByLevel()` 四个辅助方法
- `data.js`: bank 新增 `creditRatings`（AAA/AA/A/B）、`defaultCredit='B'`、`creditUpgradeDays=90`、`creditDowngradeDays=30`
- `state.js`: startGame()/migrate() 新增 `creditRating`、`creditDaysWithoutLoan`、`_creditLoanDays`
- `engine.js`: 新增 `updateCreditRating()` 每日评级更新；贷款利率改为使用信用评级倍率
- `deposit.js`: 完全重构 — 信用评级显示（AAA/AA/A/B）、滑块操作（存入/取出/贷款/还款）、每个滑块带实时标签更新
- `ui.js`: `_csUpdate`/`_csSet` 支持 `_labelUpdater` 自定义标签；新增 `_customLabelUpdater()` 方法；`capacitySlider` 初始化重置标签更新器
- `state.js`: totalAssets() 加入采矿许可证估值

## 功能五：免费招聘分档概率 + 全面优化
- 免费招聘改为4档分档随机（1.6-2.5 / 2.5-3.5 / 3.5-4.5 / 4.5-6.0，各25%），付费保持3.0-6.0均匀随机
- 旧合并逻辑加 `level` 守卫，修复新格式个体员工被错误合并为假组
- 招聘弹窗 >5人精简显示（最佳3+省略+最差2+平均加成），≤5人完整列表
- staff.js assignedList sort 改浅拷贝避免原数组突变
- 未分配列表新增"全部分配"按钮（弹出产业选择器，确认后一键分配所有未分配员工）
- 已分配每组新增"全部撤回"按钮
